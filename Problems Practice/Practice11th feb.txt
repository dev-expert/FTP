#include <iostream>
#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,l;
    cin>>n;
    int a[n],max[n],min[n];
    (n % 2) ? l=n / 2 + 1 : l=n / 2;
    cout<<"Enter array"<<endl;
    for(int i=0;i<n;i++)
    {
    cin>>a[i];
    if(i<n/2)
    {
    min[i]=a[i];
    }
    else
    {
    max[i-(n/2)]=a[i];
    }
    }
    
    for(int i=0;i<l;i++)
    {
    cout<<max[(l)-i-1]<<" ";
    if(i<n/2){
        
    
    cout<<min[i]<<" ";
    }
    }

}